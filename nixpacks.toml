# Nixpacks config — force .NET 8.0 provider so Render won't auto-detect Go
[phases.build]
cmds = ["dotnet publish CafeWebsite.csproj -c Release -o out --no-restore"]

[phases.start]
cmd = "dotnet ./out/CafeWebsite.dll"
