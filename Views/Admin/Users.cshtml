@model IEnumerable<CafeWebsite.Models.User>
@{
    ViewData["Title"] = "Users";
}

<style>
    .admin-header {
        background: linear-gradient(135deg, #000 0%, #1a0f0a 50%, #0d0705 100%);
        padding: 3rem;
        margin-bottom: 2.5rem;
        border-radius: 16px;
        color: #fff;
        box-shadow: 0 8px 32px rgba(44, 24, 16, 0.4);
        border: 1px solid rgba(212, 175, 135, 0.2);
    }
    
    .admin-header h1 {
        font-family: 'Playfair Display', Georgia, serif;
        font-size: 2rem;
        font-weight: 400;
        letter-spacing: 1px;
    }
    
    .admin-header p {
        font-size: 0.95rem;
        opacity: 0.85;
        color: #ccc;
    }
    
    .section-card {
        background: #fff;
        border: 1px solid #e8e0d8;
        border-radius: 16px;
        box-shadow: 0 4px 16px rgba(0,0,0,0.06);
        overflow: hidden;
    }
    
    .section-card .card-header {
        background: linear-gradient(135deg, #000 0%, #333 100%);
        color: #fff;
        padding: 1.25rem 1.5rem;
        border: none;
        font-family: 'Helvetica Neue', sans-serif;
        letter-spacing: 1px;
        font-size: 0.85rem;
    }
    
    .section-card .card-body {
        padding: 1.5rem;
    }
    
    .data-table {
        width: 100%;
        border-collapse: collapse;
    }
    
    .data-table th {
        background: #faf8f5;
        color: #000;
        font-weight: 700;
        padding: 1.25rem 1.5rem;
        text-align: left;
        border-bottom: 2px solid #000;
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 1px;
    }
    
    .data-table td {
        padding: 1.25rem 1.5rem;
        border-bottom: 1px solid #f0ebe5;
        color: #4a4a4a;
        font-size: 0.9rem;
        vertical-align: middle;
    }
    
    .data-table tr:last-child td {
        border-bottom: none;
    }
    
    .data-table tr:hover {
        background: #faf8f5;
    }
    
    .badge-custom {
        padding: 0.5rem 1rem;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: 600;
        text-transform: uppercase;
    }
    
    .badge-admin {
        background: linear-gradient(135deg, #000 0%, #333 100%);
        color: #fff;
    }
    
    .badge-customer {
        background: #faf8f5;
        color: #000;
        border: 1px solid #e8e0d8;
    }
    
    .badge-orders {
        background: #faf8f5;
        color: #666;
        padding: 0.4rem 0.9rem;
        font-size: 0.75rem;
        font-weight: 500;
        border: 1px solid #e8e0d8;
    }
    
    .view-btn {
        background: linear-gradient(135deg, #000 0%, #333 100%);
        color: #fff;
        padding: 0.65rem 1.25rem;
        border-radius: 20px;
        text-decoration: none;
        transition: all 0.3s ease;
        font-size: 0.8rem;
        font-weight: 500;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
    }
    
    .view-btn:hover {
        background: linear-gradient(135deg, #333 0%, #000 100%);
        color: #fff;
    }
    
    .alert-custom {
        background: linear-gradient(135deg, #000 0%, #333 100%);
        color: #fff;
        border: none;
        border-radius: 12px;
        padding: 1.25rem 1.5rem;
        margin-bottom: 1.5rem;
    }
    
    .alert-custom .btn-close {
        filter: invert(1);
    }
    
    .empty-state {
        padding: 6rem 2rem;
        text-align: center;
    }
    
    .empty-state i {
        color: #ccc;
        font-size: 4rem;
        margin-bottom: 1rem;
    }
    
    .empty-state p {
        color: #666;
        font-size: 1rem;
    }
    
    .user-avatar {
        width: 45px;
        height: 45px;
        background: linear-gradient(135deg, #000 0%, #333 100%);
        color: #fff;
        border-radius: 50%;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        font-weight: 600;
        margin-right: 0.75rem;
        font-size: 1.1rem;
    }
    
    .user-cell {
        display: flex;
        align-items: center;
    }
    
    .user-info {
        display: flex;
        flex-direction: column;
    }
    
    .user-name {
        font-weight: 600;
        color: #000;
    }
    
    .user-email {
        font-size: 0.8rem;
        color: #666;
        margin-top: 2px;
    }
    
    .stats-row {
        display: flex;
        gap: 1.5rem;
        margin-bottom: 2rem;
        flex-wrap: wrap;
    }
    
    .stat-card {
        background: #fff;
        border: 1px solid #e8e0d8;
        border-radius: 16px;
        padding: 1.5rem;
        flex: 1;
        min-width: 150px;
        display: flex;
        align-items: center;
        gap: 1.25rem;
        box-shadow: 0 4px 16px rgba(0,0,0,0.06);
    }
    
    .stat-icon {
        width: 56px;
        height: 56px;
        background: linear-gradient(135deg, #000 0%, #333 100%);
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: #fff;
        font-size: 1.4rem;
    }
    
    .stat-content h3 {
        margin: 0;
        font-size: 1.5rem;
        font-weight: 700;
        color: #000;
    }
    
    .stat-content p {
        margin: 0;
        font-size: 0.75rem;
        color: #666;
        text-transform: uppercase;
        letter-spacing: 0.5px;
    }
    
    .search-container {
        padding: 1.25rem 1.5rem;
        background: #faf8f5;
        border-bottom: 1px solid #e8e0d8;
    }
    
    .search-input {
        width: 100%;
        max-width: 300px;
        padding: 0.75rem 1rem;
        border: 1px solid #e8e0d8;
        border-radius: 25px;
        background: #fff;
        color: #000;
        font-size: 0.9rem;
    }
    
    .search-input:focus {
        outline: none;
        border-color: #000;
    }
    
    .row-actions {
        display: flex;
        gap: 0.5rem;
    }
    
    .action-icon {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        color: #fff;
        text-decoration: none;
        transition: all 0.2s ease;
        font-size: 0.9rem;
        background: linear-gradient(135deg, #000 0%, #333 100%);
    }
    
    .action-icon:hover {
        background: linear-gradient(135deg, #333 0%, #000 100%);
    }
</style>

<div class="container-fluid" style="padding: 2.5rem 2rem;">
    <!-- Admin Header -->
    <div class="admin-header">
        <div class="d-flex justify-content-between align-items-center">
            <div>
                <h1><i class="fas fa-users" style="margin-right: 0.75rem; color: #ccc;"></i> Users</h1>
                <p>Manage your customers and team members</p>
            </div>
            <div style="text-align: right;">
                <p style="margin: 0; color: #ccc;">@DateTime.Now.ToString("dddd, MMMM dd, yyyy")</p>
            </div>
        </div>
    </div>

    <!-- Stats Row -->
    <div class="stats-row">
        <div class="stat-card">
            <div class="stat-icon">
                <i class="fas fa-users"></i>
            </div>
            <div class="stat-content">
                <h3>@Model.Count()</h3>
                <p>Total Users</p>
            </div>
        </div>
        <div class="stat-card">
            <div class="stat-icon">
                <i class="fas fa-user-check"></i>
            </div>
            <div class="stat-content">
                <h3>@Model.Count(u => u.Role == "Customer")</h3>
                <p>Customers</p>
            </div>
        </div>
        <div class="stat-card">
            <div class="stat-icon">
                <i class="fas fa-user-shield"></i>
            </div>
            <div class="stat-content">
                <h3>@Model.Count(u => u.Role == "Admin")</h3>
                <p>Admins</p>
            </div>
        </div>
        <div class="stat-card">
            <div class="stat-icon">
                <i class="fas fa-coffee"></i>
            </div>
            <div class="stat-content">
                <h3>@Model.Sum(u => u.Orders?.Count ?? 0)</h3>
                <p>Total Orders</p>
            </div>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle" style="margin-right: 0.5rem;"></i>
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="section-card">
        <div class="card-header">
            <i class="fas fa-list" style="margin-right: 0.5rem;"></i> Customer & Team Directory
        </div>
        
        <!-- Search Bar -->
        <div class="search-container">
            <input type="text" class="search-input" placeholder="Search customers by name or email..." id="userSearch">
        </div>
        
        <div class="card-body">
            <div class="table-responsive">
                <table class="data-table" id="usersTable">
                    <thead>
                        <tr>
                            <th><i class="fas fa-hashtag" style="margin-right: 0.5rem;"></i>ID</th>
                            <th><i class="fas fa-user" style="margin-right: 0.5rem;"></i>Customer</th>
                            <th><i class="fas fa-envelope" style="margin-right: 0.5rem;"></i>Email</th>
                            <th><i class="fas fa-star" style="margin-right: 0.5rem;"></i>Role</th>
                            <th><i class="fas fa-shopping-bag" style="margin-right: 0.5rem;"></i>Orders</th>
                            <th><i class="fas fa-cogs" style="margin-right: 0.5rem;"></i>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr>
                                <td><span style="color: #666; font-weight: 600;">#</span><span style="color: #000;">@user.Id</span></td>
                                <td>
                                    <div class="user-cell">
                                        <div class="user-avatar">@user.Username[0].ToString().ToUpper()</div>
                                        <div class="user-info">
                                            <span class="user-name">@user.Username</span>
                                        </div>
                                    </div>
                                </td>
                                <td style="color: #4a4a4a;">@user.Email</td>
                                <td>
                                    @if (user.Role == "Admin")
                                    {
                                        <span class="badge-custom badge-admin">Admin</span>
                                    }
                                    else
                                    {
                                        <span class="badge-custom badge-customer">Customer</span>
                                    }
                                </td>
                                <td>
                                    <span class="badge-custom badge-orders">@user.Orders?.Count orders</span>
                                </td>
                                <td>
                                    <div class="row-actions">
                                        <a asp-action="UserDetails" asp-route-id="@user.Id" class="action-icon" title="View Details">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
        
        @if (!Model.Any())
        {
            <div class="empty-state">
                <i class="fas fa-users" style="font-size: 4rem; margin-bottom: 1rem;"></i>
                <p>No customers found yet. Your shop is just getting started!</p>
            </div>
        }
    </div>
</div>

<script>
    // Simple search functionality
    document.getElementById('userSearch').addEventListener('keyup', function() {
        const searchTerm = this.value.toLowerCase();
        const table = document.getElementById('usersTable');
        const rows = table.getElementsByTagName('tbody')[0].getElementsByTagName('tr');
        
        for (let i = 0; i < rows.length; i++) {
            const username = rows[i].cells[1].textContent.toLowerCase();
            const email = rows[i].cells[2].textContent.toLowerCase();
            
            if (username.includes(searchTerm) || email.includes(searchTerm)) {
                rows[i].style.display = '';
            } else {
                rows[i].style.display = 'none';
            }
        }
    });
</script>
